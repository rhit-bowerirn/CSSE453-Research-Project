from enum import Enum

class Color(Enum):
    """
    Enum Description: //TODO do this later
    """
    ROOT = (0,0,0)
    SCOUT = (0,0,255)
    NETWORKER = (0,255,0)
    TARGET = (255,0,0)
    LINE = (128,128,128)
